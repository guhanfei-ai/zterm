# Contributing to zTerm

Use `dev` for integration and short-lived `dev/<topic>` or `feature/<topic>` branches for focused changes. Keep `main` stable and merge only reviewed, passing changes. Never commit credentials, generated artifacts, or local configuration.

Run `npm ci`, `npm run verify`, and the platform build command before opening a pull request.
